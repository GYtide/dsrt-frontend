/**
 * X、Y方向游标数据横切数据的这线图的配置数据
 */

import { type } from "@testing-library/user-event/dist/type"

export const option = {
  animation: false,
  grid: [{
    left: '8%',
    right: '4%',
    top: '5%',
    bottom: '8%'
    // containLabel: true 
  }]
}